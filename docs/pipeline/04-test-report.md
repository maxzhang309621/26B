# 系统测试报告

- 项目：2026 B 题无线电干扰源定位与清除
- 范围：架构 v1.2 步骤 G–L（运行数据反演验证）
- 日期：2026-09-11
- 结论：**系统测试通过**

## 验收对照（v1.2）

| 步骤 | 用例类型 | 做法 | 结果 |
|------|----------|------|------|
| G | 正常 | 两站 `direction` 抽出坐标与示向 | 通过 |
| G | 边界 | 空日志 → 空 dict | 通过 |
| G | 异常 | `accepted=false` 被忽略；缺文件 `FileNotFoundError` | 通过 |
| H | 正常 | 无误差两站：真值在可行域内，圆心距 < 直径 | 通过 |
| I | 正常 | mock Q3 seed0/1、Q4 seed3：包含率=1；残差在 ±1.01° | 通过 |
| J | 正常 | 读 `output/drill/p3-20260911-144700.json`，20 频道，模式标明仅一致性 | 通过 |
| K | 正常 | 四类 PNG 轴带单位 (m)/(deg) | 通过 |
| L | 正常 | `--mock` / `--drill` 入口跑通 | 通过 |

## 回归

```
cd src
python -m unittest tests.test_inversion tests.test_geometry tests.test_protocol tests.test_q3 tests.test_q4 tests.test_candidate tests.test_practice_guard -q
```

**46 passed**（约 0.66 s）。未跑官方演练，未开正式测试。

## mock 定量（有真值）

| 场景 | 包含率 | ±1° 带（含舍入） | 均定位误差 |
|------|--------|------------------|------------|
| q3-seed0-n10 | 1.0 | 1.0 | 10.23 m |
| q3-seed1-n12 | 1.0 | 1.0 | 8.70 m |
| q4-seed3-n12-nd4 | 1.0 | 1.0 | 7.56 m |

产物：`output/inversion/mock_summary.json`、`drill_summary.json` 及 PNG 图。

## 问题分流

（v1.2 无开放缺陷。）

---

# 系统测试报告（v1.3 路径时效）

- 项目：2026 B 题无线电干扰源定位与清除
- 范围：架构 v1.3 步骤 M–T（问题 4 行驶时间 / 路径）
- 日期：2026-09-11
- 结论：**系统测试通过（mock）**；官方演练未跑；正式测试禁止。

## 验收对照（v1.3）

| 步骤 | 用例类型 | 做法 | 结果 |
|------|----------|------|------|
| M | 正常 | Q4 mock 台账三档加和 = travel；planned+rejoin=backbone | 通过 |
| N | 正常 | pathopt 混合源全清；远 pending 覆盖期不强制清 | 通过 |
| N | 边界 | 近心朝外仍全清 | 通过 |
| P/Q | 异常回退 | 扇区锯齿 / 贪心跳点 mock 漏清，默认 pathopt 不用 | 已记录 |
| R | 正常 | `open_path_order` 5 点置换唯一且走遍 | 通过 |
| S | 正常 | `directional_front_cover_ok()` 默认 True；dense 旗标可调用 | 通过 |
| T | 正常 | mock 24 种子 pathopt miss=0，均行驶/均 VT 低于 `run_q4()` | 通过 |
| 回归 | 正常 | `v_nofar` `test_cover_inner_before_outer` 仍约束 `run_q4()` | 通过 |

## 回归

```
cd src
python -m unittest discover -s tests -q
```

**91 passed**（约 9 s）。未跑官方演练，未开正式测试。

## mock 定量（24 种子，同一 `_mix_sources`）

| 策略 | miss | 均 VT (s) | 均行驶 (s) |
|------|------|-----------|------------|
| `run_q4()` v_nofar | 0 | 7768.7 | 6392.9 |
| `run_q4_pathopt()` | 0 | 7561.5 | 6179.0 |

Δ mean VT ≈ **−207 s**；Δ mean travel ≈ **−214 s**。未达 5000 s 目标（架构已声明非门禁）。

## 问题分流

无 L1 开放缺陷。扇区融合未达验收，已按候选回退，不升级 L2。


## 问题分流

无失败项。L1/L2/L3 未触发。

## 备注

- `svd_deg` 四舍五入到两位小数后，残差可略超 1.000°；门禁与几何核一致取 1.01°。
- 反演未接入 `HuntPolicy`。

# 系统测试报告（v1.4 交错搜索 + 先搜后清）

- 项目：2026 B 题无线电干扰源定位与清除
- 范围：架构 v1.4 步骤 U–V（问题 4 pathopt）
- 日期：2026-09-11
- 结论：**功能系统测试通过（mock 全清）**；均行驶/均 VT **尚未优于** `v_nofar`，因此 **不切换** 默认提交入口。官方演练未跑；正式测试禁止。

## 验收对照（v1.4）

| 步骤 | 用例类型 | 做法 | 结果 |
|------|----------|------|------|
| U | 正常 | `sector_fused_order` 在内环未完时出现外环点 | 通过 |
| U | 正常 | pathopt 混合源 / seed0 全清 | 通过 |
| U | 边界 | 覆盖期 `_drain_pending` 不动作；`near` 仍当场清 | 通过 |
| V | 正常 | 非听点成功 `/clear` 发生在最后一次覆盖听点扫描之后 | 通过 |
| V | 异常 | 远距离双站交会 >20 m：扇形补清 + 专用第二站 | seed0 不再漏清 |
| 回归 | 正常 | `run_q4()` 双环先内后外、混合源全清 | 通过 |
| T 对照 | 正常 | mock-24 miss=0 | 通过 |
| T 对照 | 性能 | 均行驶/均 VT 低于 v_nofar | **未通过**（不升级 L2：用户指定结构，默认入口保持 v_nofar） |

## 回归

```
cd src
python -m unittest discover -s tests -q
```

**95 passed**（约 22 s）。

## mock 定量（24 种子）

| 策略 | miss | 均 VT (s) | 均行驶 (s) |
|------|------|-----------|------------|
| `run_q4()` v_nofar | 0 | 7768.7 | 6392.9 |
| `run_q4_pathopt()` v1.4 | 0 | 8578.4 | 6679.8 |

Δ mean VT ≈ **+810 s**；Δ mean travel ≈ **+287 s**。交错主干 + 覆盖后批量定位目前比听完即清更费行驶。

## 问题分流

无 L1 开放缺陷。性能门禁未达，保持实验入口，不切换 `run_q4()`。

# 系统测试报告（交错 + 搜到即清）

- 日期：2026-09-11
- 结论：功能通过（mock 全清）；**内外交错未优化行驶**。默认入口保持 `v_nofar`。

| 策略 | miss | 均 VT (s) | 均行驶 (s) |
|------|------|-----------|------------|
| `run_q4()` 双环最近邻 + 搜到即清 | 0 | 7768.7 | 6392.9 |
| `run_q4_pathopt()` 扇区交错 + 搜到即清 | 0 | 8592.4 | 7149.8 |

95 单测通过。未跑官方演练。正式测试禁止。

# 系统测试报告（v1.5 问题 3 滚动时域清除）

- 日期：2026-09-11
- 范围：RH-OTSP 批量清除（`run_q3_batch`）；`run_q3()` 回归
- 结论：**功能通过**（mock 全清）；8 种子均 VT 相对现行 **−508 s**。默认入口仍为 `run_q3()`。

| 项 | 结果 |
|----|------|
| 单元测试 | **101 passed** |
| 截断单测 | 远第二站单步位移 ∈ (100, 400) m |
| 相位 | 离听点 >80 m 的成功 `/clear` 仍在最后一次环上 `/measure` 之后 |
| 入口 | `run_q3()` 未改 |

mock 8 局（均全清）：

| seed | n | 现行 VT | RH VT | ΔVT |
|------|---|---------|-------|-----|
| 0 | 10 | 4028 | 4759 | +731 |
| 1 | 12 | 4521 | 4076 | −446 |
| 2 | 16 | 6695 | 4970 | −1725 |
| 3 | 12 | 4366 | 3893 | −473 |
| 4 | 11 | 4129 | 3972 | −157 |
| 5 | 14 | 5338 | 4265 | −1074 |
| 6 | 10 | 4117 | 4221 | +104 |
| 7 | 13 | 4542 | 3518 | −1024 |

均值 ΔVT **−507.8 s**，Δ行驶 **−546.3 s**。源少时偶发更慢（必须先走完听点环）。未跑官方演练。正式测试禁止。

# 系统测试报告（正六边形 + 先搜后清，官方演练）

- 日期：2026-09-11 23:03
- 方案：`run_q3_batch()`，听点 `q3_waypoints()` = 原点 + 正六边形 6×1150 m，覆盖证书最坏距 988.5 m
- 入口：`drill_io.run_hunt("3")` / `run_q3_practice_batch.py`
- 结论：**官方演练 5/5 全清**，均 VT **4236.4 s**（相对旧官方约 5245 s 下降约 1000 s）。未开正式测试。

| 局 | 源数 | 清除 | VT (s) | 行驶 (s) |
|----|------|------|--------|----------|
| 1 | 12 | 12/12 | 3834 | 3174 |
| 2 | 11 | 11/11 | 4446 | 3752 |
| 3 | 10 | 10/10 | 4126 | 3431 |
| 4 | 14 | 14/14 | 4755 | 4066 |
| 5 | 13 | 13/13 | 4021 | 3367 |

单测 104 passed。`run_q3()` 仍为 8×1200 延后清除对照。问题 4 未改。
