# Q3 Step 8 开发集决策报告

- 对照：Step 7 当前默认策略；候选：clear-ready 跨未来主干边插入。
- 数据：独立本地 mock seeds `100..149`，逐 case 成对比较；未使用历史 `0..79` 作为开发证据。
- 正确性：两者均 `50/50` 全清，最大 pending 与非法动作均为 0，退出均 accepted；边界场景均 `16/16`。
- 性能：候选均值 `5118.1793 s`，基线 `5064.6180 s`，成对均值差 `+53.5613 s`（候选慢 `1.0576%`）；成对中位差 `+20.3152 s`、P90 差 `+249.5687 s`、最坏回退 `+448.1996 s`，仅 `12/50` 更快。
- 机制：177 次插入全部 clear 成功且无 fallback，但 clear-detour 均值由 `1821.4963 s` 增至 `1958.1884 s`；rejoin 与 localization 的节省不足以抵消新增清除绕行。
- 边界：固定 16 源场景改善 `358.9088 s`，但单一边界样本不能推翻独立开发集的总体与尾部回退。
- G8：正确性门槛通过，mean 改善与 P90 门槛失败。按冻结版 v3，候选不采用，不运行 locked `1000..1099`，不进入 Step 9；默认关闭的 Step 7 策略保持不变。

证据：`baseline_100_149.json`、`candidate_100_149.json`、`run_summary.json`。
